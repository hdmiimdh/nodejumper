cd && wget https://raw.githubusercontent.com/nodejumper-org/cosmos-utils/main/installation-scripts/omniflix_install.sh
mkdir -p installation-scripts && mv -f omniflix_install.sh installation-scripts/ && cd installation-scripts
chmod +x omniflix_install.sh

# replace nodejumper with your own moniker, if you'd like
./omniflix_install.sh nodejumper
