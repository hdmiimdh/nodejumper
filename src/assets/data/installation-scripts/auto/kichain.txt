cd && wget https://raw.githubusercontent.com/nodejumper-org/cosmos-utils/main/installation-scripts/kichain_install.sh
mkdir -p installation-scripts && mv -f kichain_install.sh && cd installation-scripts
chmod +x kichain_install.sh

# replace nodejumper with your own moniker, if you'd like
./kichain_install.sh nodejumper
