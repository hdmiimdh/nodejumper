cd && wget https://raw.githubusercontent.com/nodejumper-org/cosmos-utils/main/installation-scripts/rizon_install.sh
mkdir -p installation-scripts && mv -f rizon_install.sh && cd installation-scripts
chmod +x rizon_install.sh

# replace nodejumper with your own moniker, if you'd like
./rizon_install.sh nodejumper
